once login make sure
====================


1) Make sure by clicking on settings icon ( Auto Validate) check box is off. else each time you save it automatically
run validations which is risky in prod env.
